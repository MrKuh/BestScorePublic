<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.bestscore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BestScore">
        <activity android:name=".ui.home.game.GameEventActivity"></activity>
        <activity android:name=".ui.home.selection.parcour.ParcourSelection" />
        <activity android:name=".ui.home.selection.countmethod.CountMethodAdd" />
        <activity android:name=".ui.home.selection.countmethod.CountMethodSelection" />
        <activity android:name=".ui.home.game.GameSettingActivity" />
        <activity android:name=".ui.teammates.TeammateAdd" />
        <activity android:name=".ui.parcours_old.ParcourAc_old" />
        <activity android:name=".ui.home.selection.player.TeammateSelection" />
        <activity android:name=".ui.home.selection.parcour.ParcourAdd" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>